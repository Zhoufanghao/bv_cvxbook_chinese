# 第 1 章 Introduction

在本章导论中，我们概述数学优化，重点说明凸优化所扮演的特殊角色。这里以非正式方式介绍的概念，将在后续章节中以更严谨、更技术化的方式展开。

## 1.1 数学优化

一个数学优化问题，或简称优化问题，具有如下形式：

$$
\begin{array}{ll}
\text{minimize} & f_0(x) \\
\text{subject to} & f_i(x) \leq b_i,\quad i=1,\ldots,m.
\end{array}
\tag{1.1}
$$

这里，向量 $x=(x_1,\ldots,x_n)$ 是该问题的优化变量；函数 $f_0:\mathbf{R}^n\to\mathbf{R}$ 是目标函数；函数 $f_i:\mathbf{R}^n\to\mathbf{R}$，$i=1,\ldots,m$，是不等式约束函数；常数 $b_1,\ldots,b_m$ 是约束的限制值或界。若向量 $x^\star$ 在所有满足约束的向量中具有最小的目标值，则称 $x^\star$ 是最优的，或称其为问题 (1.1) 的一个解：对任意满足 $f_1(z)\leq b_1,\ldots,f_m(z)\leq b_m$ 的 $z$，都有 $f_0(z)\geq f_0(x^\star)$。

我们通常研究由目标函数和约束函数的特定形式所刻画的优化问题族或类别。一个重要例子是：如果优化问题 (1.1) 中的目标函数和约束函数 $f_0,\ldots,f_m$ 都是线性的，即满足

$$
f_i(\alpha x+\beta y)=\alpha f_i(x)+\beta f_i(y)
\tag{1.2}
$$

对所有 $x,y\in\mathbf{R}^n$ 以及所有 $\alpha,\beta\in\mathbf{R}$ 都成立，则称问题 (1.1) 为线性规划。如果优化问题不是线性的，则称为非线性规划。

本书讨论一类称为凸优化问题的优化问题。凸优化问题是目标函数和约束函数都是凸函数的问题，也就是说它们满足不等式

$$
f_i(\alpha x+\beta y)\leq \alpha f_i(x)+\beta f_i(y)
\tag{1.3}
$$

对所有 $x,y\in\mathbf{R}^n$ 以及所有满足 $\alpha+\beta=1$、$\alpha\geq0$、$\beta\geq0$ 的 $\alpha,\beta\in\mathbf{R}$ 都成立。比较 (1.3) 和 (1.2) 可以看出，凸性比线性更一般：不等式取代了更强的等式要求，而且该不等式只需对某些 $\alpha$ 和 $\beta$ 成立。因此，任何线性规划都是凸优化问题，我们可以把凸优化看作线性规划的推广。

### 1.1.1 应用

优化问题 (1.1) 抽象了这样一个问题：从一组候选选择中，在 $\mathbf{R}^n$ 中选出一个尽可能好的向量。变量 $x$ 表示所作选择；约束 $f_i(x)\leq b_i$ 表示限制可能选择的硬性要求或规格；目标值 $f_0(x)$ 表示选择 $x$ 的成本。（也可以把 $-f_0(x)$ 看作选择 $x$ 的价值或效用。）优化问题 (1.1) 的一个解，对应于在所有满足硬性要求的选择中成本最小（或效用最大）的选择。

例如，在投资组合优化中，我们希望找到把一笔资本投资到 $n$ 个资产上的最佳方式。变量 $x_i$ 表示在第 $i$ 个资产上的投资，因此向量 $x\in\mathbf{R}^n$ 描述了整个资产集合上的投资组合分配。约束可以表示预算限制（即投资总额的限制）、投资非负的要求（假设不允许做空），以及整个投资组合期望收益的最低可接受值。目标函数或成本函数可以是投资组合收益整体风险或方差的度量。在这种情况下，优化问题 (1.1) 对应于在所有满足硬性要求的可能分配中，选择一个使风险最小的投资组合分配。

另一个例子是电子设计中的器件尺寸确定，即选择电子电路中每个器件的宽度和长度。这里的变量表示器件的宽度和长度。约束表示各种工程要求，例如制造工艺施加的器件尺寸限制、确保电路能以指定速度可靠运行的时序要求，以及电路总面积的限制。器件尺寸问题中常见的目标是电路消耗的总功率。优化问题 (1.1) 就是寻找满足可制造性、时序和面积等设计要求，并且功率效率最高的器件尺寸。

在数据拟合中，任务是从一族潜在模型中找到一个最符合观测数据和先验信息的模型。这里的变量是模型中的参数，约束可以表示先验信息或对参数的必要限制（例如非负性）。目标函数可以是观测数据与模型预测值之间失配或预测误差的度量，也可以是参数值不可能性或不合理性的统计度量。优化问题 (1.1) 就是寻找与先验信息一致，并且对观测数据具有最小失配或预测误差（或在统计框架下最可能）的模型参数值。

大量涉及决策制定（或系统设计、分析和运行）的实际问题，都可以表述为数学优化问题，或表述为其某种变体，例如多准则优化问题。事实上，数学优化已经成为许多领域的重要工具。它广泛用于工程、电子设计自动化、自动控制系统，以及土木、化工、机械和航空航天工程中的最优设计问题。优化也用于网络设计与运行、金融、供应链管理、调度以及许多其他领域。应用清单仍在持续扩展。

在大多数这类应用中，数学优化作为人类决策者、系统设计者或系统操作者的辅助工具；人负责监督过程、检查结果，并在必要时修改问题（或求解方法）。这个人类决策者还会执行优化问题建议的任何动作，例如买入或卖出资产以实现最优投资组合。

一个相对较新的现象为数学优化开辟了许多其他应用的可能性。随着嵌入产品中的计算机日益普及，嵌入式优化快速增长。在这些嵌入式应用中，优化被用来自动作出实时选择，甚至执行相应动作，而无需（或只需很少）人工干预或监督。在一些应用领域，传统自动控制系统与嵌入式优化的融合已经深入推进；在另一些领域，这才刚刚开始。嵌入式实时优化提出了一些新的挑战：尤其是它要求求解方法极其可靠，并且能在可预测的时间（和内存）内求解问题。

### 1.1.2 求解优化问题

某一类优化问题的求解方法，是指这样一种算法：给定该类中的一个具体问题，即问题的一个实例，计算该问题的一个解（达到某个给定精度）。自 20 世纪 40 年代末以来，人们投入了大量精力来开发用于求解各类优化问题的算法，分析它们的性质，并开发良好的软件实现。这些算法的有效性，也就是我们求解优化问题 (1.1) 的能力，差异很大，并取决于目标函数和约束函数的具体形式、变量和约束的数量，以及稀疏性等特殊结构。（如果每个约束函数只依赖少数几个变量，则称该问题是稀疏的。）

即使目标函数和约束函数都是光滑的（例如多项式），一般优化问题 (1.1) 也出人意料地难以求解。因此，针对一般问题的方法通常都包含某种折中，例如非常长的计算时间，或可能找不到解。§1.4 将讨论其中一些方法。

不过，对于“大多数优化问题难以求解”这一一般规律，存在一些重要例外。对于少数问题类别，我们有有效算法可以可靠地求解甚至很大的问题，变量和约束可达到数百或数千个。下面 §1.2 中描述的两个重要且著名的例子（并在第 4 章中详细讨论）是最小二乘问题和线性规划。较少为人所知的是，凸优化也是这一规律的另一个例外：和最小二乘或线性规划一样，我们有非常有效的算法，可以可靠而高效地求解甚至很大的凸问题。

## 1.2 最小二乘和线性规划

本节描述凸优化中两个非常著名且广泛使用的特殊子类：最小二乘和线性规划。（这些问题的完整技术处理将在第 4 章给出。）

### 1.2.1 最小二乘问题

最小二乘问题是没有约束（即 $m=0$）的优化问题，其目标是若干形如 $a_i^T x-b_i$ 的项的平方和：

$$
\text{minimize}\quad f_0(x)=\|Ax-b\|_2^2=\sum_{i=1}^k(a_i^T x-b_i)^2.
\tag{1.4}
$$

这里 $A\in\mathbf{R}^{k\times n}$（且 $k\geq n$），$a_i^T$ 是 $A$ 的行，向量 $x\in\mathbf{R}^n$ 是优化变量。

#### 求解最小二乘问题

最小二乘问题 (1.4) 的解可以化为求解一组线性方程：

$$
(A^T A)x=A^T b,
$$

因此有解析解 $x=(A^T A)^{-1}A^T b$。对于最小二乘问题，我们有很好的算法（和软件实现）能以很高可靠性将问题求解到高精度。最小二乘问题可以在近似正比于 $n^2k$ 的时间内求解，且比例常数已知。当前的台式计算机可以在几秒内求解具有数百个变量、数千项的最小二乘问题；更强大的计算机当然可以求解更大的问题，或更快地求解同等规模的问题。（而且，根据摩尔定律，这些求解时间未来还会指数下降。）求解最小二乘问题的算法和软件已经可靠到足以用于嵌入式优化。

在许多情况下，通过利用系数矩阵 $A$ 的某些特殊结构，我们可以求解更大的最小二乘问题。例如，假设矩阵 $A$ 是稀疏的，即它的非零元素远少于 $kn$ 个。通过利用稀疏性，我们通常可以比 $n^2k$ 阶更快地求解最小二乘问题。当前的台式计算机可以在约一分钟内求解具有数万个变量、数十万项的稀疏最小二乘问题（不过这取决于具体的稀疏模式）。

对于极大规模的问题（例如有数百万个变量），或对于实时计算要求极高的问题，求解最小二乘问题仍然可能具有挑战。但在绝大多数情况下，我们可以说现有方法非常有效且极其可靠。事实上，可以说求解最小二乘问题（只要不是处在当前能力边界上的问题）已经是一项成熟技术，可以由许多不了解、也不需要了解细节的人可靠使用。

#### 使用最小二乘

最小二乘问题是回归分析、最优控制，以及许多参数估计和数据拟合方法的基础。它有许多统计解释，例如在给定被高斯测量误差污染的线性测量时，对向量 $x$ 进行最大似然估计。

把一个优化问题识别为最小二乘问题是直接的；我们只需验证目标是二次函数（然后检验相关二次型是否为正半定）。虽然基本最小二乘问题具有简单固定的形式，但在应用中会使用若干标准技术来提高其灵活性。

在加权最小二乘中，最小化加权最小二乘成本

$$
\sum_{i=1}^k w_i(a_i^T x-b_i)^2,
$$

其中 $w_1,\ldots,w_k$ 为正。（这个问题很容易表述并求解为标准最小二乘问题。）这里的权重 $w_i$ 用来反映对项 $a_i^T x-b_i$ 大小的不同关注程度，或只是用来影响解。在统计背景下，当给定的线性测量被具有不等方差的误差污染时，加权最小二乘会出现在向量 $x$ 的估计中。

最小二乘中的另一项技术是正则化，即在成本函数中加入额外项。在最简单的情况下，在成本函数中加入变量平方和的一个正倍数：

$$
\sum_{i=1}^k(a_i^T x-b_i)^2+\rho\sum_{i=1}^n x_i^2,
$$

其中 $\rho>0$。（这个问题同样可以表述为标准最小二乘问题。）额外项惩罚较大的 $x$ 值，并在只最小化第一个求和不能得到合理解的情况下产生合理解。参数 $\rho$ 由用户选择，以在使原目标函数 $\sum_{i=1}^k(a_i^T x-b_i)^2$ 较小和保持 $\sum_{i=1}^n x_i^2$ 不太大之间取得适当折中。当待估计的向量 $x$ 被赋予先验分布时，正则化会出现在统计估计中。

加权最小二乘和正则化将在第 6 章讨论；它们的统计解释将在第 7 章给出。

### 1.2.2 线性规划

另一类重要的优化问题是线性规划，其中目标函数和所有约束函数都是线性的：

$$
\begin{array}{ll}
\text{minimize} & c^T x \\
\text{subject to} & a_i^T x\leq b_i,\quad i=1,\ldots,m.
\end{array}
\tag{1.5}
$$

这里向量 $c,a_1,\ldots,a_m\in\mathbf{R}^n$ 和标量 $b_1,\ldots,b_m\in\mathbf{R}$ 是指定目标函数和约束函数的问题参数。

#### 求解线性规划

线性规划的解没有像最小二乘问题那样的简单解析公式，但有多种非常有效的求解方法，包括 Dantzig 的单纯形法，以及本书后面将描述的较新的内点法。虽然我们不能像最小二乘那样给出求解一个线性规划所需算术运算次数的精确值，但可以对使用内点法将线性规划求解到给定精度所需的运算次数建立严格界。实践中的复杂度为 $n^2m$ 阶（假设 $m\geq n$），但其常数不像最小二乘那样刻画清楚。这些算法相当可靠，尽管可能不如最小二乘方法可靠。我们可以很容易地在小型台式计算机上用几秒钟求解具有数百个变量和数千个约束的问题。如果问题是稀疏的，或具有其他可利用结构，我们通常可以求解具有数万或数十万个变量和约束的问题。

与最小二乘问题一样，求解极大规模的线性规划，或求解实时计算要求极高的线性规划，仍然是一个挑战。但和最小二乘一样，我们可以说求解（大多数）线性规划已经是一项成熟技术。线性规划求解器可以（并且已经）嵌入许多工具和应用中。

#### 使用线性规划

有些应用会直接导出形式为 (1.5) 的线性规划，或若干其他标准形式之一。在许多其他情况下，原始优化问题并不具有标准线性规划形式，但可以使用第 4 章详细介绍的技术，将其转换为等价的线性规划（然后当然就可以求解）。

作为一个简单例子，考虑 Chebyshev 逼近问题：

$$
\text{minimize}\quad \max_{i=1,\ldots,k}|a_i^T x-b_i|.
\tag{1.6}
$$

这里 $x\in\mathbf{R}^n$ 是变量，$a_1,\ldots,a_k\in\mathbf{R}^n$、$b_1,\ldots,b_k\in\mathbf{R}$ 是指定问题实例的参数。注意它与最小二乘问题 (1.4) 的相似性。对于这两个问题，目标都是项 $a_i^T x-b_i$ 大小的度量。在最小二乘中，我们使用这些项的平方和作为目标；而在 Chebyshev 逼近中，我们使用其绝对值的最大值。

另一个重要区别是，Chebyshev 逼近问题 (1.6) 的目标函数不可微；而最小二乘问题 (1.4) 的目标是二次函数，因此可微。

Chebyshev 逼近问题 (1.6) 可以通过求解如下线性规划来求解：

$$
\begin{array}{ll}
\text{minimize} & t \\
\text{subject to} & a_i^T x-t\leq b_i,\quad i=1,\ldots,k \\
& -a_i^T x-t\leq -b_i,\quad i=1,\ldots,k,
\end{array}
\tag{1.7}
$$

变量为 $x\in\mathbf{R}^n$ 和 $t\in\mathbf{R}$。（细节将在第 6 章给出。）由于线性规划很容易求解，因此 Chebyshev 逼近问题也很容易求解。

任何具有线性规划工作知识的人，都会认出 Chebyshev 逼近问题 (1.6) 是一个可以化为线性规划的问题。不过，对于没有这方面背景的人来说，Chebyshev 逼近问题 (1.6) 带有不可微目标，能够被表述并求解为线性规划，可能并不显然。

虽然识别能够化为线性规划的问题比识别最小二乘问题更复杂，但这是一项很容易掌握的技能，因为只会用到少数几种标准技巧。这项任务甚至可以部分自动化；一些用于指定和求解优化问题的软件系统，可以自动识别（某些）能够重述为线性规划的问题。

## 1.3 凸优化

凸优化问题具有如下形式：

$$
\begin{array}{ll}
\text{minimize} & f_0(x) \\
\text{subject to} & f_i(x)\leq b_i,\quad i=1,\ldots,m,
\end{array}
\tag{1.8}
$$

其中函数 $f_0,\ldots,f_m:\mathbf{R}^n\to\mathbf{R}$ 是凸的，即满足

$$
f_i(\alpha x+\beta y)\leq \alpha f_i(x)+\beta f_i(y)
$$

对所有 $x,y\in\mathbf{R}^n$ 以及所有满足 $\alpha+\beta=1$、$\alpha\geq0$、$\beta\geq0$ 的 $\alpha,\beta\in\mathbf{R}$ 都成立。最小二乘问题 (1.4) 和线性规划问题 (1.5) 都是一般凸优化问题 (1.8) 的特例。

### 1.3.1 求解凸优化问题

一般来说，凸优化问题的解没有解析公式，但（和线性规划问题一样）有非常有效的方法来求解它们。内点法在实践中表现很好，而且在某些情况下可以证明，它能以不超过问题维度某个多项式的运算次数，将问题求解到指定精度。（这将在第 11 章讨论。）

我们将看到，内点法求解问题 (1.8) 所需的步数或迭代次数几乎总是在 10 到 100 之间。忽略问题中的任何结构（例如稀疏性），每一步需要约

$$
\max\{n^3,n^2m,F\}
$$

次运算，其中 $F$ 是计算目标函数和约束函数 $f_0,\ldots,f_m$ 的一阶、二阶导数的成本。

和求解线性规划的方法一样，这些内点法相当可靠。我们可以很容易地在当前的台式计算机上，在最多几十秒内求解具有数百个变量和数千个约束的问题。通过利用问题结构（例如稀疏性），我们可以求解大得多的问题，变量和约束可达数千甚至更多。

我们还不能声称求解一般凸优化问题已经像求解最小二乘或线性规划问题那样成为成熟技术。关于一般非线性凸优化内点法的研究仍是非常活跃的研究领域，对于最佳方法或方法族尚未形成共识。但可以合理地预期，求解一般凸优化问题将在几年内成为一项技术。对于某些凸优化问题子类，例如二阶锥规划或几何规划（第 4 章将详细研究），可以说内点法正在接近成为一种技术。

### 1.3.2 使用凸优化

至少在概念上，使用凸优化与使用最小二乘或线性规划非常相似。如果我们能够把一个问题表述为凸优化问题，那么就可以高效求解它，就像可以高效求解最小二乘问题一样。稍微夸张一点说，如果你把一个实际问题表述成凸优化问题，那么你就已经解决了原问题。

当然，也存在一些重要差别。识别一个最小二乘问题是直接的，但识别一个凸函数可能很困难。此外，用于变换凸问题的技巧比变换线性规划的技巧多得多。因此，识别凸优化问题，或识别那些可以转换为凸优化问题的问题，可能具有挑战性。本书的主要目标就是为读者提供完成这项任务所需的背景。一旦掌握了识别或表述凸优化问题的技能，你会发现出人意料地多的问题可以通过凸优化来求解。

使用凸优化的挑战和艺术在于识别并表述问题。一旦完成这种表述，求解问题就像最小二乘或线性规划一样，（几乎）是一项技术。

## 1.4 非线性优化

当目标函数或约束函数不是线性的，但又不知道是否为凸时，通常使用非线性优化（或非线性规划）这个术语来描述该优化问题。遗憾的是，并不存在求解一般非线性规划问题 (1.1) 的有效方法。即使看起来简单、只有十个变量的问题也可能极具挑战，而具有几百个变量的问题可能无法处理。因此，针对一般非线性规划问题的方法采取了几种不同路线，每一种都包含某种折中。

### 1.4.1 局部优化

在局部优化中，折中之处在于放弃寻找使目标在所有可行点上最小的最优 $x$。相反，我们寻找一个仅局部最优的点，也就是说，它在其附近的可行点中使目标函数最小，但不保证其目标值低于所有其他可行点。关于一般非线性规划的大量研究都集中在局部优化方法上，因此这些方法已经发展得很充分。

局部优化方法可以很快，可以处理大规模问题，并且适用面广，因为它们只要求目标函数和约束函数可微。因此，在那些找到一个好点（即便不是最好的点）也有价值的应用中，局部优化方法被广泛使用。例如，在工程设计应用中，可以使用局部优化来改进一个最初由人工或其他设计方法得到的设计性能。

除了（可能）找不到真正的全局最优解之外，局部优化方法还有几个缺点。这些方法需要优化变量的初始猜测。这个初始猜测或起点很关键，并且会极大影响所得局部解的目标值。它们几乎不提供关于局部解距离（全局）最优还有多远的信息。局部优化方法通常对算法参数值敏感，这些参数可能需要针对某个具体问题或某个问题族进行调整。

使用局部优化方法比求解最小二乘问题、线性规划或凸优化问题更棘手。它涉及试验算法选择、调整算法参数，并寻找足够好的初始猜测（当要解一个实例时），或寻找一种生成足够好初始猜测的方法（当要解一个问题族时）。粗略地说，局部优化方法更像艺术而非技术。局部优化是一门发展充分且通常非常有效的艺术，但它仍然是一门艺术。相比之下，求解最小二乘问题或线性规划几乎不涉及什么艺术（当然，处在当前能力边界上的问题除外）。

可以对非线性规划的局部优化方法与凸优化作一个有趣的比较。由于对大多数局部优化方法而言，目标函数和约束函数可微是唯一要求，因此把一个实际问题表述为非线性优化问题相对直接。局部优化中的艺术在于问题表述之后如何求解它（这里的求解是指较弱意义上的寻找局部最优点）。在凸优化中，情形正好相反：艺术和挑战在于问题表述；一旦问题被表述为凸优化问题，求解它就相对直接。

### 1.4.2 全局优化

在全局优化中，会找到优化问题 (1.1) 的真正全局解；折中之处是效率。全局优化方法的最坏情况复杂度随问题规模 $n$ 和 $m$ 指数增长；人们希望在实践中，对于遇到的具体问题实例，该方法会快得多。虽然这种有利情况确实会出现，但并不典型。即使是只有几十个变量的小问题，也可能需要很长时间（例如数小时或数天）才能求解。

全局优化用于变量数量较少、计算时间不关键，并且找到真正全局解的价值很高的问题。工程设计中的一个例子是高价值或安全关键系统的最坏情况分析或验证。这里的变量表示不确定参数，它们可能随制造过程、环境或工作条件而变化。目标函数是一个效用函数，也就是较小值比较大值更差的函数；约束表示关于可能参数值的先验知识。优化问题 (1.1) 就是寻找参数最坏情况取值的问题。如果最坏情况值可以接受，我们就可以认证该系统相对于这些参数变化是安全或可靠的。

局部优化方法可以快速找到一组不好的参数值，但不能保证它们是绝对最坏的可能值。如果局部优化方法找到导致不可接受性能的参数值，它就成功地确定了系统不可靠。但局部优化方法不能认证系统可靠；它只能未能找到不好的参数值。相比之下，全局优化方法会找到参数的绝对最坏取值，如果相应性能可以接受，就能认证系统安全。代价是计算时间，哪怕参数数量相对较少，计算时间也可能非常大。但在认证性能的价值很高，或对可靠性、安全性判断错误的代价很高时，这可能是值得的。

### 1.4.3 凸优化在非凸问题中的作用

本书主要关注凸优化问题，以及可以化为凸优化问题的应用。但凸优化在非凸问题中也扮演着重要角色。

#### 局部优化的初始化

一个明显用途是把凸优化与局部优化方法结合起来。从一个非凸问题出发，我们首先找到该问题的一个近似但凸的表述。通过求解这个近似问题，我们可以轻松且无需初始猜测地得到该近似凸问题的精确解。然后把这个点作为局部优化方法的起点，将局部优化方法应用于原始非凸问题。

#### 非凸优化的凸启发式方法

凸优化是若干求解非凸问题的启发式方法的基础。我们将看到的一个有趣例子是寻找满足某些约束的稀疏向量 $x$（即非零元素很少的向量）的问题。虽然这是一个困难的组合问题，但存在一些基于凸优化的简单启发式方法，常常可以找到相当稀疏的解。（这些方法将在第 6 章描述。）

另一个广义例子来自随机化算法。在这种算法中，通过从某个概率分布中抽取若干候选点，并取其中最好的一个作为近似解，从而找到非凸问题的近似解。现在假设我们将从中抽取候选点的分布族是参数化的，例如由均值和协方差参数化。于是我们可以提出这样的问题：这些分布中哪一个会给出最小的目标期望值？事实证明，这个问题有时是凸问题，因此可以高效求解。（例如见习题 11.23。）

#### 全局优化的界

许多全局优化方法需要一个便宜可计算的非凸问题最优值下界。两种标准方法基于凸优化。在松弛中，每个非凸约束被替换为一个更宽松但凸的约束。在 Lagrange 松弛中，求解 Lagrange 对偶问题（第 5 章描述）。这个问题是凸的，并且为非凸问题的最优值提供一个下界。

## 1.5 本书概要

本书分为三个主要部分，标题分别为 Theory、Applications 和 Algorithms。

### 1.5.1 第一部分：Theory

在第一部分 Theory 中，我们介绍凸分析和凸优化中的基本定义、概念和结果。我们并不试图面面俱到，而是把主题选择偏向于那些我们认为有助于识别和表述凸优化问题的内容。这些是经典材料，几乎都可以在其他凸分析与优化教材中找到。我们并不试图给出结果的最一般形式；读者若需要，可以参考任何一本标准凸分析教材。

第 2 章和第 3 章分别讨论凸集和凸函数。我们给出若干常见的凸集和凸函数例子，以及若干凸演算规则，即保持集合和函数凸性的运算。把基本例子与凸演算规则结合起来，可以构造（或也许更重要的是识别）一些相当复杂的凸集和凸函数。

在第 4 章 Convex optimization problems 中，我们仔细处理优化问题，并描述若干可用于重新表述问题的变换。我们还介绍一些常见的凸优化子类，例如线性规划和几何规划，以及较新发展起来的二阶锥规划和半定规划。

第 5 章讨论 Lagrange 对偶性，它在凸优化中起核心作用。这里我们给出经典的 Karush-Kuhn-Tucker 最优性条件，以及凸优化问题的局部和全局灵敏度分析。

### 1.5.2 第二部分：Applications

在第二部分 Applications 中，我们描述凸优化在概率与统计、计算几何、数据拟合等领域中的各种应用。我们希望以广泛读者都能理解的方式描述这些应用。为了使每个应用保持简短，我们只考虑简单情形，有时会加入关于可能扩展的评论。我们确信，我们对某些应用的处理会让专家皱眉，对此我们预先致歉。但我们的目标是快速地、面向广泛读者传达该应用的风味，而不是给出优雅、理论上严密或完整的处理。我们自己的背景是电气工程，领域包括控制系统、信号处理、电路分析与设计。尽管我们在所教课程中包含这些主题（以本书为主要教材），但其中只有少数应用足够容易被广泛读者理解，因而被收入这里。

第二部分的目标是通过实例向读者展示凸优化如何在实践中应用。

### 1.5.3 第三部分：Algorithms

在第三部分 Algorithms 中，我们描述求解凸优化问题的数值方法，重点放在 Newton 算法和内点法。第三部分组织为三章，分别讨论无约束优化、等式约束优化和不等式约束优化。这些章节遵循一种自然层次结构：求解一个问题被化为求解一系列更简单的问题。二次优化问题（例如包括最小二乘）构成该层次结构的基础；它们可以通过求解一组线性方程精确求解。第 9 章和第 10 章发展的 Newton 方法是该层次结构的下一层。在 Newton 方法中，求解无约束或等式约束问题被化为求解一系列二次问题。在第 11 章中，我们描述内点法，它构成该层次结构的最高层。这些方法通过求解一系列无约束或等式约束问题来求解不等式约束问题。

总体而言，我们只介绍少数几种算法，并省略整类优秀方法，例如拟 Newton、共轭梯度、束方法和割平面算法。对于我们所描述的方法，我们给出的是简化变体，而不是最新、最复杂的版本。我们选择算法时遵循了几个标准。我们选择那些简单（便于描述和实现）、同时可靠稳健，并且对大多数问题足够有效和快速的算法。

许多凸优化用户最终会使用（而不是开发）标准软件，例如线性规划或半定规划求解器。对于这些用户，第三部分的材料旨在传达这些方法的基本风味，并给出它们基本属性的一些认识。对于少数最终会开发新算法的读者，我们认为第三部分可以作为一个良好的入门。

### 1.5.4 附录

本书有三个附录。第一个附录列出我们使用的一些基本数学事实，并兼有说明本书记号的作用。第二个附录讨论一个相当特定的主题：具有二次目标和一个二次约束的优化问题。这些问题是非凸的，但仍然可以有效求解，我们在第二部分描述的若干应用中会使用这些结果。

最后一个附录简要介绍数值线性代数，重点关注那些能够利用问题结构（例如稀疏性）来提高效率的方法。我们不讨论若干重要主题，包括舍入误差分析，也不给出执行所需分解的方法细节。这些主题已有许多优秀教材覆盖。

### 1.5.5 关于例子的说明

在正文许多地方（尤其是第二部分和第三部分，它们分别讨论应用和算法），我们用具体例子说明思想。在某些情况下，例子是专门选择（或设计）来说明我们观点的；在另一些情况下，例子被选择为“典型”的。这意味着这些例子是从某个显然或简单的概率分布中抽样得到的。根据几十个或几百个随机生成例子来推断算法性能的危险是众所周知的，因此我们不在这里重复。这些例子只是为了给出算法性能的大致概念，或给出计算工作量如何随问题维度变化的大致概念，而不是算法性能的精确预测。特别地，你得到的结果可能与我们的不同。

### 1.5.6 关于习题的说明

每章最后都有一组习题。有些习题要求补全正文中某个论证或断言的细节。另一些习题集中于判断或证明给定集合、函数或问题的凸性；或者更一般地，集中于凸优化问题表述。有些章节包含数值习题，需要使用适当的高级语言进行一些（但不多）编程。习题难度混合，而且会毫无预警地从相当直接变为颇为棘手。

## 1.6 记号

我们的记号大体上是标准的，但有少数例外。本节描述我们的基本记号；更完整的列表见第 697 页。

我们用 $\mathbf{R}$ 表示实数集，用 $\mathbf{R}_+$ 表示非负实数集，用 $\mathbf{R}_{++}$ 表示正实数集。实 $n$ 维向量集合记为 $\mathbf{R}^n$，实 $m\times n$ 矩阵集合记为 $\mathbf{R}^{m\times n}$。我们用方括号界定向量和矩阵，元素之间用空格分隔。我们用圆括号从逗号分隔的列表构造列向量。例如，若 $a,b,c\in\mathbf{R}$，则有

$$
(a,b,c)=
\begin{bmatrix}
a\\
b\\
c
\end{bmatrix}
=[a\ b\ c]^T,
$$

它是 $\mathbf{R}^3$ 中的元素。符号 $\mathbf{1}$ 表示所有分量都为 1 的向量（维度由上下文确定）。记号 $x_i$ 可以指向量 $x$ 的第 $i$ 个分量，也可以指集合或向量序列 $x_1,x_2,\ldots$ 中的第 $i$ 个元素。上下文或正文会说明其含义。

我们用 $\mathbf{S}^k$ 表示对称 $k\times k$ 矩阵集合，用 $\mathbf{S}_+^k$ 表示对称正半定 $k\times k$ 矩阵集合，用 $\mathbf{S}_{++}^k$ 表示对称正定 $k\times k$ 矩阵集合。弯曲不等号 $\succeq$（以及其严格形式 $\succ$）用于表示广义不等式：在向量之间，它表示逐分量不等式；在对称矩阵之间，它表示矩阵不等式。带下标时，符号 $\preceq_K$（或 $\prec_K$）表示关于锥 $K$ 的广义不等式（见 §2.4.1）。

我们描述函数的记号与标准记号略有不同，但希望不会造成混淆。我们用记号 $f:\mathbf{R}^p\to\mathbf{R}^q$ 表示 $f$ 是定义在 $\mathbf{R}^p$ 某个子集上的 $\mathbf{R}^q$ 值函数；这个子集就是它的定义域，记为 $\operatorname{dom} f$。可以把我们使用的记号 $f:\mathbf{R}^p\to\mathbf{R}^q$ 看作函数类型声明，就像计算机语言中的声明一样：$f:\mathbf{R}^p\to\mathbf{R}^q$ 表示函数 $f$ 接受一个实 $p$ 维向量作为参数，并返回一个实 $q$ 维向量。集合 $\operatorname{dom} f$，即函数 $f$ 的定义域，指定了 $\mathbf{R}^p$ 中那些使 $f(x)$ 有定义的点。作为例子，我们把对数函数描述为 $\log:\mathbf{R}\to\mathbf{R}$，且 $\operatorname{dom}\log=\mathbf{R}_{++}$。记号 $\log:\mathbf{R}\to\mathbf{R}$ 表示对数函数接受并返回一个实数；$\operatorname{dom}\log=\mathbf{R}_{++}$ 表示对数只在正数上有定义。

我们把 $\mathbf{R}^n$ 用作一般有限维向量空间。我们会遇到若干其他有限维向量空间，例如给定最高次数的一元多项式空间，或对称 $k\times k$ 矩阵空间 $\mathbf{S}^k$。通过为一个向量空间指定基，我们总可以把它与 $\mathbf{R}^n$（其中 $n$ 是它的维数）等同起来，因此针对向量空间 $\mathbf{R}^n$ 陈述的一般结果也可以应用。我们通常把将一般结果或陈述翻译到其他向量空间的工作留给读者。例如，任意线性函数 $f:\mathbf{R}^n\to\mathbf{R}$ 都可以表示为 $f(x)=c^T x$，其中 $c\in\mathbf{R}^n$。对向量空间 $\mathbf{S}^k$ 的对应陈述可以通过选择一个基并进行翻译得到。结果是：任意线性函数 $f:\mathbf{S}^k\to\mathbf{R}$ 都可以表示为 $f(X)=\operatorname{tr}(CX)$，其中 $C\in\mathbf{S}^k$。

## Bibliography

最小二乘是一个非常古老的主题；例如可参见 Gauss 在 1820 年代用拉丁文写成、近来由 Stewart 翻译的专著 [Gau95]。较新的工作包括 Lawson 和 Hanson [LH95] 以及 Björck [Bjö96] 的书。关于线性规划的参考文献见第 4 章。

关于非线性规划局部方法，有许多优秀教材，包括 Gill、Murray 和 Wright [GMW81]，Nocedal 和 Wright [NW99]，Luenberger [Lue84]，以及 Bertsekas [Ber99]。

全局优化见 Horst 和 Pardalos [HP94]、Pinter [Pin95] 以及 Tuy [Tuy98] 的书。使用凸优化为非凸问题寻找界是一个活跃研究主题，上述全局优化书籍、Ben-Tal 和 Nemirovski 的书 [BTN01, §4.3]，以及 Nesterov、Wolkowicz 和 Ye 的综述 [NWY00] 都涉及这一主题。该主题上一些著名论文包括 Goemans 和 Williamson [GW95]，Nesterov [Nes00, Nes98]，Ye [Ye99]，以及 Parrilo [Par03]。随机化方法见 Motwani 和 Raghavan [MR95]。

凸分析，即凸集、凸函数和优化问题的数学，是数学中发展充分的一个子领域。基本参考文献包括 Rockafellar [Roc70]，Hiriart-Urruty 和 Lemaréchal [HUL93, HUL01]，Borwein 和 Lewis [BL00]，以及 Bertsekas、Nedić 和 Ozdaglar [Ber03] 的书。更多关于凸分析的参考文献可在第 2-5 章找到。

Nesterov 和 Nemirovski [NN94] 最早指出内点法可以求解许多凸优化问题；另见第 11 章中的参考文献。Ben-Tal 和 Nemirovski 的书 [BTN01] 覆盖现代凸优化、内点法及其应用。

本书未覆盖的凸优化求解方法包括次梯度方法 [Sho85]、束方法 [HUL93]、割平面方法 [Kel60, EM75, GLY96]，以及椭球方法 [Sho91, BGT81]。

凸优化问题可处理这一思想并不新鲜。人们早已认识到，凸优化理论比一般非线性优化理论直接（且完整）得多。在这一背景下，Rockafellar 在其 1993 年 SIAM Review 综述论文 [Roc93] 中说：

> In fact the great watershed in optimization isn’t between linearity and nonlinearity, but convexity and nonconvexity.

关于凸优化问题比一般非线性优化问题更容易求解的第一个形式化论证，由 Nemirovski 和 Yudin 在其 1983 年的书 *Problem Complexity and Method Efficiency in Optimization* [NY83] 中给出。他们表明，凸优化问题的基于信息的复杂度远低于一般非线性优化问题。关于这一主题的较新书籍是 Vavasis [Vav91]。

内点法较低的（理论）复杂度是该领域现代研究的核心。许多研究集中于证明某种内点法（或其他方法）可以用不快于问题维度和 $\log(1/\epsilon)$ 的多项式增长的运算次数，求解某一类凸优化问题，其中 $\epsilon>0$ 是所需精度。（我们将在第 11 章看到一些类似的简单结果。）关于这一主题的第一部综合性著作是 Nesterov 和 Nemirovski 的书 [NN94]。其他书籍包括 Ben-Tal 和 Nemirovski [BTN01, lecture 5] 以及 Renegar [Ren01]。各种凸优化问题的内点法多项式时间复杂度，与许多非凸优化问题形成鲜明对比；对于后者，所有已知算法在最坏情况下所需的运算次数都随问题维度指数增长。

凸优化已经用于众多应用领域，数量太多，无法在这里一一引用。凸分析在经济学和金融学中处于核心地位，是许多结果的基础。例如，分离超平面定理与无套利假设一起，可用于推出价格和风险中性概率的存在性（例如见 Luenberger [Lue95, Lue98] 和 Ross [Ros99]）。凸优化，尤其是我们求解半定规划的能力，最近在自动控制理论中受到特别关注。凸优化在控制理论中的应用可见 Boyd 和 Barratt [BB91]，Boyd、El Ghaoui、Feron 和 Balakrishnan [BEFB94]，Dahleh 和 Diaz-Bobillo [DDB95]，El Ghaoui 和 Niculescu [EN00]，以及 Dullerud 和 Paganini [DP00] 的书。嵌入式（凸）优化的一个好例子是模型预测控制，这是一种自动控制技术，每一步都需要求解一个（凸）二次规划。模型预测控制现在已广泛用于化工过程控制行业；见 Morari 和 Zafírou [MZ89]。凸优化（尤其是几何规划）具有悠久历史的另一个应用领域是电子电路设计。关于这一主题的研究论文包括 Fishburn 和 Dunlop [FD85]，Sapatnekar、Rao、Vaidya 和 Kang [SRVK93]，以及 Hershenson、Boyd 和 Lee [HBL01]。Luo [Luo03] 对信号处理和通信中的应用作了综述。更多关于凸优化应用的参考文献可见第 4 章和第 6-8 章。

用于凸优化问题的近期内点法已有高质量实现，见 LOQO [Van97] 和 MOSEK [MOS02] 软件包，以及第 11 章列出的代码。用于指定优化问题的软件系统包括 AMPL [FGK99] 和 GAMS [BKMR98]。二者都对识别可转换为线性规划的问题提供了一些支持。
